# git || project:toolchain.git +st

## Reset to HEAD
*  git reset `--hard` HEAD  

## branches
* branch commmand
    * list branches
        * `git branch -l`
* creating
    * `git checkout -b <branch_name>` 

